{
  "name": "accounting",
  "version": "1.0.0",
  "description": "Student Account Management System — Node.js port of the legacy COBOL application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --verbose"
  },
  "dependencies": {
    "readline-sync": "^1.4.10"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
